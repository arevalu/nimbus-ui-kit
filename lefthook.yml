pre-commit:
  parallel: true
  commands:
    lint:
      glob: "src/**/*.{js,ts,tsx}"
      run: npx eslint {files}
    typescript:
      glob: "*.{js,ts, jsx, tsx}"
      run: npx tsc --noEmit
commit-msg:
  parallel: true
  commands:
    commitlint:
      run: npx commitlint --edit
