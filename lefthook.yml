pre-commit:
  parallel: true
  commands:
    lint:
      run: npx eslint 'src/**/*.{js,ts,tsx}' --max-warnings=0 --cache
    typescript:
      glob: "*.{js,ts, jsx, tsx}"
      run: npx tsc --noEmit
commit-msg:
  parallel: true
  commands:
    commitlint:
      run: npx commitlint --edit
